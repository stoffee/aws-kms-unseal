output "connections" {
  value = <<VAULT
Connect to Vault via SSH   ssh -i yoursshkey ubuntu@${aws_instance.vault[0].public_ip} 
Vault web interface  http://${aws_instance.vault[0].public_ip}:8200/ui
VAULT

}